%% dont re-indent this file
\begin{lstlisting}[style=reclojureClojure]
(defn large [n]
  (and (typep n 'Integer)
       (> n 127)))

(defn even [n]
  (and (typep n 'Integer)
       (= 0 (mod n 2))))
\end{lstlisting}
